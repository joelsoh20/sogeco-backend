package com.sogeco.fleet.common.enums;

/** Actions RH tracees depuis la fiche chauffeur (RG-9.12). */
public enum DriverActionType {
    PRIME,
    AVERTISSEMENT,
    FORMATION,
    ENTRETIEN,
    FELICITATION
}
