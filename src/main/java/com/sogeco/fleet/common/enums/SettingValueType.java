package com.sogeco.fleet.common.enums;

/** Type de la valeur d'un parametre systeme, pour sa conversion. */
public enum SettingValueType {
    STRING,
    INTEGER,
    DECIMAL,
    BOOLEAN,
    JSON
}
