package com.sogeco.fleet.common.enums;

/** Porteur d'un document, pour la table polymorphe. */
public enum EntityType {
    VEHICLE,
    DRIVER,
    MISSION,
    MAINTENANCE,
    INSURANCE,
    INSPECTION,
    CLAIM,
    FUEL,
    OTHER
}
